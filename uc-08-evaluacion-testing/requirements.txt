# Python deps para la suite de evaluación de UC-08.
# Instalar dentro de un venv:
#   cd uc-08-evaluacion-testing
#   python3 -m venv venv && source venv/bin/activate
#   pip install -r requirements.txt
#
# Nota: ragas y deepeval traen dependencias pesadas (torch, transformers).
# La primera instalación tarda; subsecuentes corridas son rápidas.

ragas>=0.2.0
datasets>=2.14.0
anthropic>=0.40.0
requests>=2.31.0

# Opcional — solo si vas a usar las métricas avanzadas de DeepEval además de las heurísticas locales
deepeval>=2.0.0
